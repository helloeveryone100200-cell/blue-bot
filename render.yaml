services:
  - type: web
    name: blue-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: MONGO_URI
        sync: false
      - key: OWNER_ID
        value: "1827336632"
      - key: ADMIN_GROUP_ID
        value: "-1001234567890"
      - key: BOT_USERNAME
        sync: false
      - key: PORT
        value: "8080"
